---
apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - script: |
      kubectl create configmap test-scripts \
        --namespace $NAMESPACE \
        --from-file=test-export.sh \
        --from-file=create-snapshot.hbase \
        --from-file=delete-snapshot.hbase \
        --from-file=restore-snapshot.hbase
