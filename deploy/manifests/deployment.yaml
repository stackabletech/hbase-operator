---
# =============
# This file is automatically generated from the templates in stackabletech/operator-templating
# DON'T MANUALLY EDIT THIS FILE
# =============
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hbase-operator-deployment
  labels:
    app.kubernetes.io/name: hbase-operator
    app.kubernetes.io/instance: hbase-operator
    app.kubernetes.io/version: "0.2.0-nightly"
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: hbase-operator
      app.kubernetes.io/instance: hbase-operator
  template:
    metadata:
      labels:
        app.kubernetes.io/name: hbase-operator
        app.kubernetes.io/instance: hbase-operator
    spec:
      serviceAccountName: hbase-operator-serviceaccount
      securityContext: {}
      containers:
        - name: hbase-operator
          securityContext: {}
          image: "docker.stackable.tech/stackable/hbase-operator:0.2.0-nightly"
          imagePullPolicy: IfNotPresent
          resources: {}
          volumeMounts:
            - mountPath: /etc/stackable/hbase-operator/config-spec
              name: config-spec
      volumes:
        - name: config-spec
          configMap:
            name: hbase-operator-configmap
